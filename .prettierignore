dist/
.astro/
package-lock.json

# Content collections are authored by non-technical editors (GitHub web editor /
# Pages CMS). Don't format-check their Markdown — a deploy must never fail over
# content whitespace. Astro validates these files' frontmatter at build instead.
src/content/
# Event details, edited via Pages CMS or by hand — same reasoning; src/data/event.ts
# validates its values at build. (Only this data file, not the rest of src/data/.)
src/data/event.json
public/fonts/
*.woff2
*.webp
*.jpg
*.png
*.svg
